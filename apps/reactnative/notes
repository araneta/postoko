How to build
nvm use v22.14.0 

export JAVA_HOME=/media/araneta/49909430-d2bd-4bcf-be1d-3c425a4013bf/apps/jdk-17.0.11
export PATH=$PATH:$HOME/bin:$JAVA_HOME/bin

Android SDK Platform 35
export ANDROID_HOME=$HOME/Android/Sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:/home/araneta/.nvm/versions/node/v22.14.0/bin/node
export ANDROID_SDK_ROOT=$HOME/Android/Sdk

keytool -genkey -v -keystore my-release-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000
npm install
npx expo install --check
npx expo prebuild --clean
cd android/
./gradlew bundleRelease

#apk
./gradlew assembleRelease


if error duplicate class then update gradle.properties add:
 android.enableJetifier=true

jika error node > A problem occurred starting process 'command 'node''

sudo ln -s "$(which node)" /usr/local/bin/node

apk location
/android/app/build/outputs/apk/release
